class Solution {
    public int rob(int[] nums) {
        int t1 = 0, t2 = 0;
        if (nums.length == 1) {
            return nums[nums.length - 1];
        }

        return Math.max(rob(nums.));
    }
}